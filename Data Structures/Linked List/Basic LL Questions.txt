
## 🧵 Singly Linked List (20 Questions)

1. Implement insertion at the beginning of a singly linked list.  
2. Implement insertion at the end of a singly linked list.  
3. Insert a node at a given position in a singly linked list.  
4. Delete the first node of a singly linked list.  
5. Delete the last node of a singly linked list.  
6. Delete a node at a given position in a singly linked list.  
7. Search for an element in a singly linked list.  
8. Count the number of nodes in a singly linked list.  
9. Reverse a singly linked list (iterative).  
10. Reverse a singly linked list (recursive).  
11. Find the middle element of a singly linked list.  
12. Detect if a cycle exists in a singly linked list (Floyd’s algorithm).  
13. Remove a cycle from a singly linked list.  
14. Merge two sorted singly linked lists into one sorted list.  
15. Find the intersection point of two singly linked lists.  
16. Remove duplicates from a sorted singly linked list.  
17. Remove duplicates from an unsorted singly linked list.  
18. Find the Nth node from the end of a singly linked list.  
19. Rotate a singly linked list by k positions.  
20. Check if a singly linked list is a palindrome.

---

## 🔗 Doubly Linked List (15 Questions)

21. Insert a node at the head of a doubly linked list.  
22. Insert a node at the tail of a doubly linked list.  
23. Insert a node at a given position in a doubly linked list.  
24. Delete the head node of a doubly linked list.  
25. Delete the tail node of a doubly linked list.  
26. Delete a node at a given position in a doubly linked list.  
27. Traverse a doubly linked list forward.  
28. Traverse a doubly linked list backward.  
29. Reverse a doubly linked list.  
30. Delete all occurrences of a given key in a doubly linked list.  
31. Find pairs in a doubly linked list with a given sum.  
32. Convert a doubly linked list to a balanced BST.  
33. Implement an LRU Cache using a doubly linked list + HashMap.  
34. Insert a node before a given node in a doubly linked list.  
35. Insert a node after a given node in a doubly linked list.

---

## 🔄 Circular Linked List (15 Questions)

36. Insert a node at the beginning of a circular linked list.  
37. Insert a node at the end of a circular linked list.  
38. Insert a node at a given position in a circular linked list.  
39. Delete the head node of a circular linked list.  
40. Delete the tail node of a circular linked list.  
41. Delete a node at a given position in a circular linked list.  
42. Traverse a circular linked list.  
43. Split a circular linked list into two halves.  
44. Convert a singly linked list into a circular linked list.  
45. Detect a loop in a circular linked list.  
46. Remove a loop in a circular linked list.  
47. Implement the Josephus problem using a circular linked list.  
48. Insert a node in a sorted circular linked list.  
49. Delete all occurrences of a given key in a circular linked list.  
50. Reverse a circular linked list.

